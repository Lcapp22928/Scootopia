using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Powerups : MonoBehaviour
{
   /*[SerializeField]  private float _speedIncreaseAmount = 10;
   [SerializeField]  private float _powerupDuration = 3;

   private void OnTriggerEnter (Collider other){
    ScooterMovement player1 =other.gameObject.GetComponent<ScooterMovement>();
    if (player1 !=null){
        // powerup sequence
        StartCoroutine(PowerupSequence(player1));
    }
   } 

   public IEnumerator PowerupSequence(ScooterMovement player1){
    // activate

    // wait for amount of time
    yield return new WaitForSeconds(_powerupDuration);
    // deactivate

   }  */
}
